#include "main.h"

/**
 * _abs - Computes the absolute value of an integer.
 *
 * @x: Character to print
 *
 * Return: Numer -1, 0, 1
 */
int _abs(int j)
{
	if (j > 0)
	{
		return (j);
	}
	else
	{
		return (j * -1);
	}
	_putchar('\n');
}
