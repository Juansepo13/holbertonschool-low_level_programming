#include "main.h"

/**
 * _puts - prints the text pass by reference
 * @s: pointer to char
 */

void _puts(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		_putchar(s[i]);
	}
	_putchar('\n');

}
