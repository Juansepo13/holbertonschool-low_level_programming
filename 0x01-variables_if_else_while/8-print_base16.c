#include <stdio.h>
/**
 * main - Main function
 *
 * Return: return 0
 */

int main(void)
{
	char q;
	int w;

	for (w = 0; w < 10; w++)
		putchar(w + '0');

	for (q = 'a'; q <= 'f'; q++)
		putchar(q)

			putchar('\w');

	reurn (0);
}
