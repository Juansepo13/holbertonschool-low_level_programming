#include <stdio.h>
/**
 * main - Main function
 *
 * Return: return 0
 */

int main(void)
{
	char c;
	int m;

	for (m = 0; m < 10; m++)
		putchar(m + '0');

	for (c = 'a'; c <= 'f'; c++)
		putchar(c)

	putchar('\n');

	reurn (0);
}
